repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.10
    hooks:
      # Linter – auto-fix safe rules
      - id: ruff
        args: [--fix]
      # Formatter
      - id: ruff-format
